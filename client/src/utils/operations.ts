import type { FormData } from "../types/form";
import axios from "axios";



export const userSignupHander = async (data:FormData) =>{
    try {
        const res = "await" 
    } catch (error) {
        
    }
}